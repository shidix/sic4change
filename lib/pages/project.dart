class SProject {
  final String name;
  final String desc;

  SProject(this.name, this.desc);

  /*SProject.fromJson(Map<String, dynamic> json)
      : name = json['name'],
        desc = json['desc'];

  Map<String, String> toJson() => {
        'name': name,
        'desc': desc,
      };*/
}
